{
  "name": "smartschedule",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"python -m uvicorn app.main:app --reload --port 8000 --app-dir backend\" \"cd frontend && pnpm dev\"",
    "dev:backend": "python -m uvicorn app.main:app --reload --port 8000 --app-dir backend",
    "dev:frontend": "cd frontend && pnpm dev",
    "test": "python -m pytest backend/tests/ -v",
    "install:all": "pip install -r backend/requirements.txt && cd frontend && pnpm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
