{
  "name": "smartschedule",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"cd backend && uvicorn app.main:app --reload --port 8000\" \"cd frontend && corepack pnpm dev\"",
    "dev:backend": "cd backend && uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && corepack pnpm dev",
    "test": "cd backend && python -m pytest tests/ -v",
    "install:all": "cd backend && pip install -r requirements.txt && cd ../frontend && corepack pnpm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}